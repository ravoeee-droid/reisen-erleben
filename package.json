{
  "name": "reisen-erleben-premium-v2",
  "version": "2.0.0",
  "private": true,
  "description": "Editorial high-end conversion prototype for Reisen & Erleben Motorradreisen Werner GmbH",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && python3 -m http.server 3000 -d dist"
  },
  "engines": { "node": ">=20" }
}

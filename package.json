{
  "name": "reisen-erleben-premium-concept",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && python3 -m http.server 3000 -d dist"
  },
  "engines": {
    "node": ">=20"
  }
}
